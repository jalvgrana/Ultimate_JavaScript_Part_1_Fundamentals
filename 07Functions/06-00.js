const  person = {
    firstName :"Mosh",
    lastName : 'Hamedani'
};

console.log(person.firstName + " " + person.lastName)
//better way: using a template literal
console.log(`${person.firstName} ${person.lastName}`);